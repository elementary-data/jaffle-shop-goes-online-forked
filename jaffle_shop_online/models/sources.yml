version: 2

sources:
  - name: elementary
    description: "Elementary data observability platform metadata and monitoring data"
    schema: "{{ target.schema }}"
    tables:
      - name: elementary_exposures
        description: "Elementary platform exposure definitions and metadata for data lineage and impact analysis"