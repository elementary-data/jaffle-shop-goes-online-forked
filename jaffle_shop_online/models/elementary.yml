version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - pii
                  - staging
              meta:
                owner:
                  - idan+999@elementary-data.com
      - name: order_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - pii
                  - staging
              meta:
                owner:
                  - idan+999@elementary-data.com
