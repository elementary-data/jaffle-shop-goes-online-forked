version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - customer_success
                severity: warn
              meta:
                owner:
                  - joost
      - name: order_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - customer_success
                severity: warn
              meta:
                owner:
                  - joost
