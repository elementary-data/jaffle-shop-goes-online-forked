version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - orders
                severity: warn
              meta:
                owner:
                  - joost@elementary-data.com
      - name: order_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - orders
                severity: warn
              meta:
                owner:
                  - joost@elementary-data.com
