version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - stability
                severity: warn
              meta:
                owner:
                  - Idan
      - name: order_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - stability
                severity: warn
              meta:
                owner:
                  - Idan
