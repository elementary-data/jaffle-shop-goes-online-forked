version: 2
models:
  - name: order_items
    columns:
      - name: order_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - elementary-tests
                error_if: '>500'
              meta:
                owner:
                  - joost@elementary-data.com
      - name: customer_id
        data_tests:
          - unique:
              description: The column should contain only unique values
              config:
                tags:
                  - elementary-tests
                error_if: '>500'
              meta:
                owner:
                  - joost@elementary-data.com
